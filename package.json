{
  "name": "surfmail",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "jest src",
    "ts-node": "ts-node",
    "build": "rm -rf build/* && rm -rf functions/* && tsc && cp build/functions/*.js functions/",
    "prestart": "npm run build",
    "start": "twilio-run start",
    "predeploy": "npm run build",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.3.0",
    "axios": "^1.4.0",
    "dotenv": "^16.3.1",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "twilio-run": "^3.5.2",
    "typescript": "^5.1.3"
  },
  "engines": {
    "node": "16"
  }
}
