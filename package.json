{
  "name": "surfmail",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "jest src",
    "ts-node": "ts-node",
    "clean": "rm -rf build/* && rm -rf functions/*",
    "prestart": "npm run clean && tsc && cp build/functions/*.js functions/",
    "start": "twilio-run start",
    "predeploy": "npm run clean && tsc && cp build/functions/*.js functions/",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.3.0",
    "axios": "^1.4.0",
    "dotenv": "^16.3.1",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "@types/jest": "^29.5.2",
    "ts-jest": "^29.1.0",
    "jest": "^29.5.0",
    "twilio-run": "^3.5.2",
    "typescript": "^5.1.3"
  },
  "engines": {
    "node": "16"
  }
}
